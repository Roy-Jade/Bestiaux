services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: bestiaux
      POSTGRES_PASSWORD: bestiaux
      POSTGRES_DB: bestiaux
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
